<header>
  <im class="logo" src="{{ 'logo.png' | asset_img_url }}" />
  <ul class="nav-bar">
    <!-- Main menu -->
    {% for link in linklists.main-menu.links -%}
      {{ link.title | link_to: link.url }}
    {%- endfor %}

    <!-- Footer menu -->
    {% for link in linklists['footer'].links -%}
      {{ link.title | link_to: link.url }}
    {%- endfor %}
  </ul>
</header>